[package]
name = "bd-client"
version = "0.1.0"
edition = "2024"
rust-version = "1.87"
description = "Typed process interface for the Beads CLI"
license = "MIT"

[dependencies]
fs2 = "0.4"
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
tempfile = "3.20"

[dev-dependencies]
